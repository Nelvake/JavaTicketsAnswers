Билет № 4.

1. Java 8. Optional<T> - что за класс, для чего используется, основные методы.
2. DeadLock – что это такое, и как избежать.
3. Spring – возможные способы внедрения зависимости.


1. Optional класс обертка, в который оборачиваются другие объекты по типу T.
			или
Optional — объект-контейнер, используемый для хранения ненулевых объектов. 

Используется для предотвращения exception NullPointerException.

ifPresent() - Метод позволяет выполнить какое-то действие, если объект не пустой.
isPresent() - Этот метод возвращает ответ, существует ли искомый объект или нет, в виде Boolean.

orElse() — возвращает объект по дефолту.
orElseGet() — вызывает указанный метод.
orElseThrow() — выбрасывает исключение.

get() — возвращает объект, если он есть.
map() — преобразовывает объект в другой объект.
filter() — фильтрует содержащиеся объекты по предикату.
flatmap() — возвращает множество в виде стрима.

2. Взаимная блокировка (deadlock) - Она происходит, когда потоки имеют циклическую зависимость от пары синхронизированных объектов.

Решения deadlock
- Иерархия ресурсов
- Дополнительный монитор
- Гибкий захват ресурсов (java.util.concurrent.locks.Lock – tryLock, wait time lock)

3. 
• Конструктор (как советует официальная документация).  Поставить аннотацию @Autowired над конструктором.
• Сеттер. Поставить аннотацию @Autowired над сеттером.
• Поле(field) или свойство(property). Поставить аннотацию @Autowired над полем или над свойством.

То же самое, но в xml:
1. <bean class="ru.javalang.injection.<Название класса>">
    <constructor-arg ref="engine" />
</bean>
2.  <bean class="ru.javalang.injection.<Название класса>">
    <property name="engine" ref="engine" />
</bean>